<h1>Create a review for this <%= @list.name %> </h1>

<%= simple_form_for([@list, @review]) do |f|%>
  <%= f.input :comment %>
  <%= f.submit "Submit", class: "btn btn-default btn-primary m-3" %>
  <%= link_to "Cancel", list_path(@list), class: "btn btn-light" %>
<% end  %>
